{
  "name": "beds24-calendar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "encrypt": "node --env-file=.env scripts/encrypt-token.mjs",
    "dev": "node --env-file=.env scripts/encrypt-token.mjs && vite",
    "build": "node scripts/encrypt-token.mjs && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "flatpickr": "^4.6.13"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
